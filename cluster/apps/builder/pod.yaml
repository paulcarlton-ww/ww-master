apiVersion: v1
kind: Pod
metadata:
  name: builder
  labels:
    app: builder
spec:
  containers:
  - image: pcarlton/tekton-buildkit-builder:0.1.0
    command:
      - "sleep"
      - "604800"
    imagePullPolicy: IfNotPresent
    name: builder
  restartPolicy: Always